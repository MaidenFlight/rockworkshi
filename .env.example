# Used by docker-compose.yml to configure the Postgres container.
# Copy to .env and fill in real values (or leave these defaults for local dev).
POSTGRES_USER=rockworks
POSTGRES_PASSWORD=changeme
POSTGRES_DB=rockworks_dev
POSTGRES_PORT=5432
